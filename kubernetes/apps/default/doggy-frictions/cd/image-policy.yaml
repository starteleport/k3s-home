---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: cluster-apps-doggy-frictions
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cluster-apps-doggy-frictions
    namespace: flux-system
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+'
  policy:
    semver:
      range: ">=0"
